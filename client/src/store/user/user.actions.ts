import { Action, Dispatch } from "redux";
import { AppState } from "../types";
import { LoginParams } from "./user.types";

export const login = (params: LoginParams) => (dispath: Dispatch, state: AppState): Action => {

}
